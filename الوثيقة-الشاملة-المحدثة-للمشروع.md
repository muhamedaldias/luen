# الوثيقة الشاملة المحدّثة — مشروع محرر الصور الويب
### (تدمج الوثيقة الأصلية + كل الإصلاحات وخطة التنفيذ المتفق عليها لاحقاً)

هذه النسخة تحل محل الوثيقة السابقة. أي عضو في الفريق يعود لها فقط، ولا يحتاج مراجعة المحادثات القديمة.

---

## 1. نظرة عامة على المشروع

محرر صور ويب يحاكي المحررات المشهورة (Photopea, Pixlr) مع تركيز خاص على معالجة الصور وتحسينها وإزالة الخلفية ودمج الصور. الفريق 3 أشخاص. **إضافة جديدة مهمة**: المشروع يُبنى ليعمل **من كود واحد كتطبيق ويب وكتطبيق سطح مكتب معاً** (وليس نسختين منفصلتين).

**المتطلبات الدنيا المطلوبة أصلاً:**
- تحويل بين صيغ الصور
- إزالة الخلفية
- الرسم على الصور
- عمليات الصورة: قص، تكبير، تصغير، دوران، تحويل، قلب
- تحسينات صورة بالبكسل (سطوع/تباين/تشبع/حدة)
- فلاتر (تنعيم وتحديد)
- دمج صورتين
- اختيار خلفية من مكتبة خلفيات منتجات (أو توليدها)
- + ميزات إبداعية إضافية بعد تحقيق الحد الأدنى

---

## 2. القرارات التقنية النهائية (Stack)

| المكون | الاختيار النهائي | السبب |
|---|---|---|
| Frontend | **React + Vite** (بدون Next.js) | تجنب تعقيد SSR/hydration مع Fabric.js |
| التوجيه | **لا توجيه (صفحة واحدة: المحرر)** | React Router مؤجَّل مع صفحات الدخول/المعرض — الإضافة الآن تضخّم الحزمة بلا فائدة |
| مكتبة Canvas | **Fabric.js v6.4** (المثبتة فعلياً — v7 لم تُعتمد بعد) | Object Model ناضج، فلاتر، رسم حر، SVG/ClipPath |
| Backend | **FastAPI** | async للـI/O، Pydantic، Swagger مدمج |
| تنفيذ المعالجة الثقيلة | **`run_in_executor` مع `ProcessPoolExecutor`** (مُحدَّث — كان Thread افتراضياً) | راجع القسم 15-أ للتفصيل الكامل |
| قاعدة البيانات | **SQLite** عبر **SQLAlchemy + Alembic** | التبديل لاحقاً لـPostgreSQL = تغيير `DATABASE_URL` فقط |
| التخزين | مجلد محلي `/storage/{user_id}/{image_id}` | كافٍ لهذا الحجم |
| إزالة الخلفية | **`rembg`** (نموذج `isnet-general-use`، احتياطي `u2net`) | الوحيد من أدوات AI المعتمد الآن — راجع القسم 12 |
| معالجة الصور | Pillow + OpenCV (تقسيم أدوار أدناه) | Pillow للبسيط، OpenCV للتحكم الدقيق |
| سطح المكتب | **Tauri + PyInstaller** | راجع القسم 11 المُحدَّث |

---

## 3. تقسيم الأدوار: Pillow مقابل OpenCV

**Pillow** (`ImageEnhance`, `ImageFilter`, `ImageOps`): تحويل الصيغ (فحص التوقيع لا الامتداد)، Brightness/Contrast/Saturation/Sharpness، فلاتر presets (BLUR, SHARPEN, EDGE_ENHANCE, EMBOSS, CONTOUR, SMOOTH)، autocontrast, equalize, invert, posterize, solarize, mirror/flip.

**OpenCV** (`imgproc` + `photo`): تحويلات ألوان (RGB↔HSV↔LAB↔Grayscale)، Affine/Perspective transform، Gaussian/Median/Bilateral blur وSharpening بـkernels مخصصة، عمليات مورفولوجية لتنظيف أقنعة إزالة الخلفية، كشف حواف (Canny/Sobel/Laplacian)، Thresholding (Otsu/Adaptive)، Histogram Equalization/CLAHE، Inpainting كلاسيكي، Denoising، **`cv2.seamlessClone`** لدمج صورتين.

---

## 4. تفصيل تنفيذ كل متطلب أدنى

| الميزة | التقنية |
|---|---|
| تحويل الصيغ | Pillow (خادم) + `canvas.toDataURL()` (عميل) |
| إزالة الخلفية | `rembg` + تنظيف القناع بعمليات مورفولوجية |
| الرسم | Fabric.js `PencilBrush` + `freeDrawingMode` |
| قص/تكبير/تصغير/دوران/تحويل/قلب | Fabric.js (معاينة فورية) + Pillow/OpenCV (تنفيذ نهائي)، `INTER_AREA` للتصغير و`INTER_LANCZOS4` للتكبير |
| تحسينات البكسل | Pillow `ImageEnhance` أو LAB (تعديل قناة L فقط) |
| فلاتر تنعيم/تحديد | OpenCV، تُنفَّذ على الخادم |
| دمج صورتين | Fabric.js Layers + `cv2.seamlessClone` |
| مكتبة خلفيات | ألوان/تدرجات/أنماط مولّدة برمجياً + drop shadow كلاسيكي |

---

## 5. معايير الجودة العالية

- معالجة داخلية بدقة **float32**، تحويل لـuint8 فقط عند الحفظ النهائي
- الحفاظ على **قناة Alpha** طوال خط الأنابيب
- عدم استخدام JPEG كصيغة وسيطة (PNG فقط، JPEG عند التصدير النهائي)
- خوارزمية resize مناسبة حسب الاتجاه
- تعديل التباين/السطوع عبر LAB وليس RGB مباشرة

---

## 6. معمارية الباك اند: "سجل العمليات" (Operation Registry)

كل عملية = endpoint منفصل، مربوطة في قاموس بدل if/else طويلة:

```python
OPERATIONS = {
    "crop": classical_crop,
    "sharpen": classical_sharpen,
    "auto_enhance": classical_clahe,
    "remove_background": call_rembg,
}

@app.post("/api/operations/{operation_name}")
async def run_operation(operation_name: str, image_id: str, params: dict):
    handler = OPERATIONS.get(operation_name)
    if not handler:
        raise HTTPException(404, "عملية غير معروفة")
    loop = asyncio.get_running_loop()
    result = await loop.run_in_executor(process_pool, handler, image_id, params)
    return {"status": "done", "result_url": result}
```

**لماذا هذا النمط**: قابلية تبديل المحرك لاحقاً بتغيير سطر واحد، وهو نفسه ما تحتاجه ميزة الوكيل الذكي مستقبلاً (كل مفتاح = أداة جاهزة).

---

## 7. معمارية "تشغيل عدة محركات بالتوازي" (Fan-out/Fan-in)

تُستخدم فقط لميزة أو ميزتين كحد أقصى (المرشح: إزالة الكائن)، حيث النتيجة تختلف بصرياً بين الكلاسيكي وAI:

```python
COMPARISON_OPERATIONS = {
    "remove_object": [
        {"engine": "opencv_classical", "handler": classical_inpaint},
        {"engine": "iopaint_lama", "handler": call_iopaint},  # مؤجل، راجع القسم 12
    ]
}

@app.post("/api/compare/remove_object")
async def remove_object_compare(image_id: str, mask: dict):
    tasks = [run_engine_safely(e["handler"], image_id, mask) for e in COMPARISON_OPERATIONS["remove_object"]]
    results = await asyncio.gather(*tasks, return_exceptions=True)
    return [{"engine": e["engine"], "result_url": r}
            for e, r in zip(COMPARISON_OPERATIONS["remove_object"], results)
            if not isinstance(r, Exception)]
```

لا يُطبَّق على عمليات لها إجابة رياضية واحدة صحيحة (قص، دوران).

---

## 8. قاعدة البيانات — محلية وقابلة للتوسع

SQLAlchemy ORM، `DATABASE_URL` كمتغير بيئة، تجنّب ميزات خاصة بـSQLite، Alembic من البداية.

**إضافة جديدة (راجع القسم 15-هـ)**: عند حذف مشروع/صورة، يجب حذف الملفات الفعلية من `/storage/` وليس فقط سجل قاعدة البيانات (Cascade Delete)، لتفادي تراكم ملفات يتيمة على القرص.

---

## 9. تعدد الصفحات

تعدد الصفحات **مؤجَّل**: التطبيق حالياً صفحة واحدة (المحرر فقط). عند الحاجة: React Router بدون Next.js لصفحات تسجيل الدخول ولوحة/معرض المشاريع.

---

## 10. لماذا لا Next.js حالياً

`next/image` لا يفيد داخل Canvas تفاعلي، Fabric.js يحتاج احتياطات إضافية مع SSR. الهجرة لاحقاً ممكنة بجهد يوم-3 أيام إن حافظتم على فصل واضح لمكونات Fabric.js.

---

## 11. تطبيق سطح المكتب — من نفس الكود (مُحدَّث ومُفصَّل)

**الفكرة الجوهرية**: ليس بناء تطبيقين، بل **نفس المستودع بالضبط**، يُبنى مرتين بمخرجين مختلفين:

- **الفرونت-إند**: نفس React بلا تغيير. الفرق الوحيد هو متغير بيئة `VITE_API_URL`:
  - بناء الويب → `https://your-domain.com/api`
  - بناء سطح المكتب → `http://localhost:8000/api`
- **الباك-إند**: نفس كود FastAPI. في نسخة سطح المكتب، يُحوَّل عبر **PyInstaller** لملف تنفيذي مستقل لا يحتاج بايثون مثبتاً لدى المستخدم، ويُشغَّل تلقائياً كعملية فرعية ("Sidecar" في مصطلح Tauri) عند فتح التطبيق على `localhost:8000`، ويُوقَف عند الإغلاق.
- **نافذة العرض**: Tauri لا يحمل متصفحاً كاملاً (خلافاً لـElectron) بل يستخدم محرك العرض المدمج بالنظام (WebView2/WebKit) — لذلك حجمه 10-20 ميجابايت فقط مقابل 150-300 لـElectron.

**ملاحظة على نموذج rembg (~44-176 ميجابايت حسب النموذج المختار)**: إما يُضمَّن داخل ملف التثبيت (يعمل بدون إنترنت من أول تشغيل، حجم أكبر) أو يُنزَّل عند أول تشغيل.

**توصية توقيت التنفيذ (جديدة)**: لا تؤجلوا تجربة التغليف الكامل (Tauri+PyInstaller) للنهاية. جرّبوها مبكراً كـ"تجربة دخان" بعد أول ميزة حقيقية تعمل (راجع المرحلة 7 في القسم 16) لأن PyInstaller قد لا يكتشف تلقائياً مكتبات مثل OpenCV/rembg (تحتاج `--add-data`/`--hidden-import` صريحة)، وأفضل اكتشاف ذلك مبكراً لا في آخر يوم قبل التسليم.

---

## 12. الذكاء الاصطناعي — القرار النهائي المحدَّث

### المعتمد الآن: rembg فقط

**ما هو**: مكتبة بايثون (`pip install rembg`) تعمل فوق **ONNX Runtime** (لا تحتاج PyTorch كامل ولا GPU). تُستخدم كمكتبة، CLI، أو خادم HTTP. أول استخدام يُنزِّل النموذج تلقائياً ويُخزَّن محلياً (`~/.u2net/`).

**النماذج المتوفرة داخلها**:

| النموذج | الحجم التقريبي | الاستخدام الأنسب |
|---|---|---|
| `u2net` | ~176 م.ب | عام، أعلى دقة، الافتراضي |
| `u2netp` | ~4.7 م.ب | نسخة مصغّرة وأسرع |
| `u2net_human_seg` | متوسط | صور بشرية |
| `u2net_cloth_seg` | ~168 م.ب | تقطيع الملابس |
| `silueta` | ~43 م.ب | مضغوط للحجم |
| **`isnet-general-use`** ✅ المُختار | ~44-170 م.ب | الأحدث، حواف أدق (شعر مثلاً) — **التوصية الأساسية** |
| `isnet-anime` | ~168 م.ب | شخصيات أنمي |
| `sam` | كبير | عام جداً لكن أثقل — **غير مستخدم من داخل rembg الآن** |

**السرعة على CPU**: مقبولة جداً — حوالي **1-2 ثانية لكل صورة** على معالج عادي عبر ONNX Runtime (بالمقارنة، بدائل مبنية على PyTorch كامل أخذت 10-44 ثانية في اختبارات مشابهة). **قرار سليم تماماً — هذا الوحيد من قسم الذكاء الاصطناعي القديم الذي يعمل بسرعة معقولة بدون GPU.**

**نصيحة تنفيذية حاسمة**: حمّلوا الجلسة (`new_session("isnet-general-use")`) **مرة واحدة عند إقلاع الخادم**، لا في كل طلب — إعادة إنشائها في كل استدعاء تُضيف بطئاً وهمياً غير حقيقي.

### المؤجَّل عمداً (يُضاف مستقبلاً عند الحاجة/توفر GPU)
LaMa عبر IOPaint (إزالة كائن)، Real-ESRGAN (تكبير)، SAM/SAM2 (تحديد ذكي)، GFPGAN/CodeFormer (ترميم وجوه)، DeOldify/DDColor (تلوين)، Stable Diffusion (توليد خلفية من نص).

**السبب**: كل هذه النماذج تفترض ضمنياً GPU للعمل بسرعة تفاعلية مقبولة (SAM وStable Diffusion على CPU قد تأخذ ثوانٍ إلى دقائق للصورة الواحدة) — غير عملي بدون توفر GPU فعلي للفريق.

---

## 13. ميزة الوكيل الذكي (AI Agent) — مرحلة لاحقة، بلا تغيير

نافذة يربط فيها المستخدم مفتاح API الخاص به، يكتب طلباً بلغة طبيعية، والنموذج ينفّذه عبر Function Calling عبر نفس عمليات الـregistry (قسم 6). **ملاحظة أمان مؤجَّلة** (راجع القسم 15-و): طريقة حفظ/تمرير مفتاح API تحتاج قراراً معمارياً عند التنفيذ الفعلي، لا الآن.

---

## 14. تصميم الواجهة (UI/UX)

**مصدر الحقيقة الآن هو Figma** (التصميم جاهز). التخطيط: شريط قوائم علوي، شريط أدوات عمودي أيسر، كانفاس مركزي، لوحة يمنى بتبويبات (Layers/Adjustments/History)، شريط حالة سفلي اختياري، لوحة إضافية قابلة للطي محجوزة لنافذة الوكيل الذكي مستقبلاً.

**نظام الألوان**: خلفية دافئة قريبة من الأسود (`#121212`-`#141312`)، أسطح مرتفعة `#1C1B1A` بحد `#2A2927`، نص أساسي `#EDEBE7` وثانوي `#9C9891`، لون مميز واحد (تراكوتا `#C97B4A` أو أخضر-تركواز `#5FA88E`)، ألوان دلالية مخفَّفة.

**أدوات البناء**: Tailwind CSS، مكونات واجهة مخصصة بأسلوب المشروع (لم يُعتمد shadcn/ui فعلياً — كان مقترحاً في التصميم الأولي)، react-resizable-panels، Lucide Icons (outline فقط).

**تجنّبوا صراحة**: تدرجات بنفسجي-أزرق زخرفية، Glassmorphism مفرط، أكثر من لون مميز واحد بنفس الشاشة، أيقونات مختلطة الأنماط، إيموجي داخل الواجهة.

---

## 15. الإصلاحات والتحسينات المتفق عليها (قسم جديد كامل)

### أ) استبدال `run_in_executor` الافتراضي بـ`ProcessPoolExecutor`

**المشكلة**: `run_in_executor(None, ...)` يستخدم افتراضياً **ThreadPoolExecutor** — خيوط تشترك في نفس العملية وتتقيّد بـ**GIL بايثون** (خيط واحد فقط ينفّذ bytecode بايثون في لحظة معينة). معالجة الصور (rembg/OpenCV/Pillow) هي عمل **CPU-bound** خالص، فهذا لا يعطي توازياً حقيقياً على الأنوية المتعددة عند تزامن عدة طلبات.

**الحل**:
```python
from concurrent.futures import ProcessPoolExecutor
process_pool = ProcessPoolExecutor(max_workers=4)  # حسب أنوية الخادم

loop = asyncio.get_running_loop()
result = await loop.run_in_executor(process_pool, handler, image_id, params)
```

**الفرق**: ProcessPoolExecutor يشغّل كل مهمة في **عملية منفصلة تماماً** بمفسّر بايثون خاص بها، فيتجاوز الـGIL كلياً ويستغل الأنوية فعلياً. **لماذا هذا "الأفضل" وليس Celery/Redis الآن**: يحل المشكلة بسطرين، بدون بنية تحتية جديدة (Broker/Worker منفصل)، بروح تجنّب الهندسة الزائدة المتبعة في باقي القرارات. الانتقال لـCelery لاحقاً يكون فقط عند حاجة حِمل حقيقية (طوابير أولوية، إعادة محاولة تلقائية، توزيع على خوادم منفصلة).

### ب) حل حجم/أبعاد الصورة — يُطبَّق الآن (وليس مؤجَّلاً)

مشكلة أعمق من حد حجم الملف: **Decompression Bomb** — ملف صغير على القرص يفكّ إلى أبعاد ضخمة في الذاكرة عند فتحه.

```python
from PIL import Image
import io

Image.MAX_IMAGE_PIXELS = 64_000_000   # يُضبط عالمياً عند إقلاع التطبيق
MAX_FILE_SIZE_MB = 15
MAX_DIMENSION = 8000                   # بكسل لكل ضلع

def validate_uploaded_image(file_bytes: bytes):
    if len(file_bytes) > MAX_FILE_SIZE_MB * 1024 * 1024:
        raise HTTPException(413, "حجم الملف يتجاوز الحد المسموح")
    try:
        img = Image.open(io.BytesIO(file_bytes))
        img.verify()
    except Exception:
        raise HTTPException(400, "الملف ليس صورة صالحة")
    img = Image.open(io.BytesIO(file_bytes))  # يُعاد الفتح بعد verify()
    if img.width > MAX_DIMENSION or img.height > MAX_DIMENSION:
        raise HTTPException(400, f"الأبعاد تتجاوز {MAX_DIMENSION}×{MAX_DIMENSION}")
    return img
```

هذا الفحص **أول شيء** في endpoint الرفع، قبل أي معالجة، ومستقل تماماً عن نظام المستخدمين — لذلك يُنفَّذ الآن حتى لو باقي الفجوات الأمنية مؤجَّلة.

### ج) آلية إعلام الواجهة بانتهاء المعالجة غير المتزامنة: Polling بسيط

القاعدة العامة: WebSocket لاتصال ثنائي الاتجاه عالي التكرار (شات، تعاون لحظي) — **ليس وضعنا**. لتحديث حالة مهمة خلفية واحدة، النمط القياسي هو **Polling**:

```
POST /api/operations/{name}            → {"job_id": "...", "status": "processing"}
GET  /api/operations/{job_id}/status   → تُستدعى كل 1-1.5 ثانية من الواجهة
                                          → {"status": "done", "result_url": "..."} أو {"status": "processing"}
```

يُطبَّق فقط على العمليات الأبطأ من نصف ثانية (rembg وما شابه) — العمليات الفورية (قص، دوران) تبقى synchronous بلا `job_id`. **ترقية مستقبلية إن احتجتم**: SSE (Server-Sent Events) بدل Polling المتكرر — أبسط بكثير من WebSocket.

### د) Undo/Redo — Memento محدود العمق

**كيف تفعله فوتوشوب/فيجما فعلياً**: نمط **Command** (تخزين كل عملية وعكسها فقط، لا الحالة الكاملة) — لأن مستنداتهم ضخمة جداً بحيث أن حفظ لقطة كاملة لكل تغيير يُنهك الذاكرة. فيجما تحديداً يستخدم سجل عمليات (Operation Log) بسبب طبيعته التعاونية اللحظية.

**التوصية الواقعية لحجم مشروعكم** (وليس تقليد فوتوشوب حرفياً — استثمار غير مبرر لحجمكم):
- استخدموا **Memento مبسّط** عبر `canvas.toJSON()` / `canvas.loadFromJSON()` المدمجة في Fabric.js — لقطة بعد كل عملية تعديل (لا كل حركة فأرة).
- **حدّوا عمق المكدس** (آخر 20-30 حالة فقط، الأقدم تُحذف تلقائياً) لتفادي استهلاك الذاكرة.
- هذا تنازل هندسي واعٍ ومقبول تماماً لحجم مشروع تخرج، لا نقصاً.

### هـ) سياسة الاحتفاظ بالبيانات: Cascade Delete فقط

عند حذف مشروع/صورة من لوحة المشاريع، احذفوا الملفات الفعلية من `/storage/{user_id}/{image_id}` وليس فقط سجل قاعدة البيانات (عبر event listener في SQLAlchemy) — لتفادي تراكم ملفات يتيمة على القرص. لا حاجة لنظام حذف تلقائي أعقد من هذا لمشروع تخرج.

### و) حدود أداء Fabric.js: فصل دقة العرض عن دقة المستند

**ما تفعله Photopea فعلياً**: تفصل **دقة العرض التفاعلي** عن **دقة المستند الحقيقية**. الحل لكم: حدّوا العرض التفاعلي في Fabric.js بحد أقصى (مثلاً 4096 بكسل لأطول ضلع) بغض النظر عن الأبعاد الحقيقية المرفوعة (حتى 8000 كما في البند ب)، بينما كل عملية نهائية (قص/فلتر/إزالة خلفية) تُنفَّذ على الخادم على الملف الأصلي كامل الدقة، وتُعاد النتيجة.

### ز) استراتيجية اختبار خفيفة

`pytest` + `TestClient` من FastAPI (مبني في Starlette، بلا تثبيت إضافي تقريباً). تُغطى: رفع صورة صالحة/غير صالحة، كل عملية في الـregistry تُرجع نتيجة منطقية، حالات حدود البند ب. يبدأ بها الشخص المسؤول عن التكامل/الاختبار فوراً موازاة مع بناء الميزات.

### ح) الفجوات الأمنية المؤجَّلة (بقرار صريح — تُضاف بعد إنجاز البرنامج الأساسي)
- صفحة تسجيل الدخول واستراتيجية المصادقة (JWT أو Session)
- طريقة حفظ/تمرير مفتاح API لميزة الوكيل الذكي بأمان
- التحقق الصارم من صلاحية الوصول للملفات (مطابقة `user_id` في كل endpoint لمنع ثغرات IDOR)

---

## 16. خطة التنفيذ المرحلية النهائية (بعد جاهزية تصميم Figma)

| # | المرحلة | لماذا بهذا الترتيب تحديداً |
|---|---|---|
| 0 | تأسيس المستودع + اختبار `/health` بين الطرفين | يكشف مشاكل البنية التحتية (CORS، الاتصال) مبكراً وبتكلفة زهيدة، قبل استثمار وقت في الميزات |
| 1 | استخراج نظام التصميم من Figma إلى `tailwind.config.js` وإعادة تلوين shadcn/ui فوراً | يمنع الاعتماد على ألوان shadcn الافتراضية "مؤقتاً" ثم نسيان تغييرها — التبديل يصبح مستحيل الفشل من اليوم الأول |
| 2 | الهيكل البصري الفارغ (Shell) مطابقاً لـFigma بدون أي منطق فعلي | يفصل مشكلة "هل التخطيط صحيح بصرياً؟" عن "هل المنطق يعمل؟" فيسهل تتبع الأخطاء لاحقاً |
| 3 | أول شريحة عمودية كاملة: رفع صورة (بفحوصات القسم 15-ب) → عرضها في Fabric.js (بدقة معاينة محدودة، القسم 15-و) | هذا العمود الفقري الذي تعتمد عليه كل ميزة لاحقة بلا استثناء |
| 4 | إثبات نمط الـRegistry بعملية واحدة بسيطة فقط (مثلاً القص) من البداية للنهاية، مستخدمة `ProcessPoolExecutor` | يختبر المعمارية الكاملة أفقياً مرة واحدة بدل اكتشاف نفس المشكلة 8 مرات لاحقاً من 3 أشخاص بافتراضات مختلفة |
| 5 | توسيع بقية العمليات بالتوازي بين الأعضاء + Polling للعمليات الأبطأ + دمج rembg | آمنة للتوازي الحقيقي بين الأعضاء الآن لأن النمط مُثبَت من المرحلة 4 |
| 6 | Undo/Redo (Memento محدود العمق، القسم 15-د) | يعتمد على استقرار شكل حالة الكانفاس بعد استقرار العمليات — بناؤه قبلها يعني إعادة كتابته لاحقاً |
| 7 | تجربة تغليف Tauri+PyInstaller مبكراً كـ"تجربة دخان"، ثم تغليف نهائي كامل قرب التسليم | PyInstaller قد لا يكتشف مكتبات مثل OpenCV/rembg تلقائياً — اكتشاف ذلك مبكراً بينما الوقت متاح للتصحيح، لا في آخر يوم |

---

## 17. أخطاء يجب تجنبها (محدَّثة)

- تشغيل معالجة الصور مباشرة داخل async route بدون executor، **أو استخدام ThreadPoolExecutor الافتراضي لعمل CPU-bound خالص** (استخدموا ProcessPoolExecutor)
- فقدان قناة Alpha بين خطوات المعالجة
- استخدام JPEG كصيغة وسيطة أثناء المعالجة
- **عدم ضبط `Image.MAX_IMAGE_PIXELS`** (وليس فقط تحديد حجم الملف بالميجابايت) — يفتح الباب لهجوم Decompression Bomb
- إهمال إعداد CORS
- استخدام ميزات خاصة بـSQLite تمنع الترحيل لاحقاً
- تطبيق نمط "تشغيل عدة محركات" على كل شيء بدل ميزة أو ميزتين فقط
- استخدام ثيم shadcn الافتراضي بدون إعادة تلوين
- **حذف سجل من قاعدة البيانات دون حذف الملف الفعلي من `/storage/`** (ملفات يتيمة)
- **بناء Undo/Redo قبل استقرار شكل العمليات النهائي**
- **تأجيل تجربة تغليف Tauri للنهاية تماماً** بدل تجربة دخان مبكرة

---

## 18. ملخص كل ما أُجِّل عمداً لمراحل لاحقة (مُحدَّث)

- الهجرة إلى Next.js (لو احتُجنا API Routes/SSR فعلياً)
- ميزة الوكيل الذكي (AI Agent panel) — البنية جاهزة لها عبر نمط الـregistry
- نماذج AI الأثقل: LaMa/IOPaint، Real-ESRGAN، SAM/SAM2، GFPGAN/CodeFormer، DeOldify/DDColor، Stable Diffusion — تُضاف عند توفر GPU فعلي
- **الفجوات الأمنية**: تسجيل الدخول والمصادقة، أمان مفتاح API للوكيل، التحقق الصارم من صلاحية الوصول للملفات (`user_id`)
- MinIO / PostgreSQL خارجية / Redis+Celery — فقط عند حاجة فعلية لحمل حقيقي
- Docker Compose + CI/CD الكاملة — آخر مرحلة
- الميزات الإبداعية الثقيلة (توليد خلفيات AI، تعاون لحظي، تلوين، ترميم وجوه) — ميزة أو ميزتين كحد أقصى
